# Aus Ouvidos

## Run the application

### Option #1: Using the built-in run command

* Run the application in VSCode or Visual Studio 2017 by hitting `F5`

### Option #2: Using the command line

* Run the .NET application using `dotnet run`
